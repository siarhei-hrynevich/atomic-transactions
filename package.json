{
  "name": "atomic-transactions",
  "version": "1.0.0",
  "description": "Transactions for execution atomic tasks",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "npm run clean && tsc -p .",
    "clean": "rm -r dist",
    "test": "jest --collect-coverage"
  },
  "repository": "https://github.com/siarhei-hrynevich/atomic-transactions",
  "author": "Siarhei Hrynevich",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^25.5.4",
    "ts-jest": "^25.5.1",
    "typescript": "^4.6.3"
  }
}
